name: bug_writer
description: Local-first CLI for drafting, validating, and submitting Bug Wall bugs.
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  args: ^2.5.0
  postgres: ^3.3.0

executables:
  bug_writer: bug_writer
